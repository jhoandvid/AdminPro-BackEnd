const Usuario=require('../models/usuario');
const usuario = require('../models/usuario');
const Hospital=require('../models/usuario')
const Usuario=require('../models/usuario')

const actualizarImagen=(tipo, id, path, nombreArchivo)=>{

    const usaurio=JSON.parse(tipo);

    console.log(usuario)
    

}






module.exports={
    actualizarImagen
}
